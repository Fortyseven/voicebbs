const { postRouter } = require("./post");
const { postsRouter } = require("./posts");
const { postBlobRouter } = require("./post-blob");

module.exports = {
    postRouter,
    postsRouter,
    postBlobRouter,
};
